
Download a Project
git clone https://github.com/manelcolominas/Questions-Answers.git
git pull origin main

git tag -> per saber amb quina versió estas treballant
git checkout tags/VersionConMultiplesFormularios


Abans de treballar en els arxius 
git pull -> per a descarregar-te la ultima versió del projecte

Actualizar arxius GitHub

git add .
git pull origin main -> per a baixar-te els canvis que s'han fet al repositori abans de pujar el teu
git commit -m "Descripción breve de los cambios"
git push origin main

git tag v1.0
git push origin v1.0


Crear repositori

mkdir ejemplo
cd ejemplo/
git init -> crear un nou repositori
-rf -git/ -> destruir el contingut de la carpeta del repositori de github

hash -> es per saber si el contingut d'un objecte ha canviat o no. per això guardarem els hash al git per saber si un projecte ha canviat o no o si esteu treballant en un mateix projecte igual.

commit -> així es com es diuen les versions a git
status -> 
git add <filename> -> per afegir un cert arxiu
primer commit -> git commit -m "initial commit"
log
push -> per a carregar els commits al reposiori de github
pull -> per a descarregar els arxius del repositori de github

Insights -> network graph

merge -> juntar els canvis fets pel teu company i tu, unificar branques

git tindra problemes   quan treballem en el mateix arxiu





